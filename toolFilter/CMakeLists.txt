cmake_minimum_required(VERSION 2.8)
project( toolFilter )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( toolFilter toolFilter.cpp )
target_link_libraries( toolFilter ${OpenCV_LIBS} )